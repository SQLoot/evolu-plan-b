---
"@evolu/common": minor
---

Added `allSettled`, `forEach`, `any`, and `withConcurrency` to `Task` module.
Updates documentation conventions and improved lazy docs.
Polyfilled `Promise.withResolvers` and `Promise.try` for compatibility.
Fixed build errors related to strict type checking.
