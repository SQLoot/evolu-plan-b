[API Reference](/docs/api-reference/page.mdx) / [@evolu/common](/docs/api-reference/common/page.mdx) / [Platform](/docs/api-reference/common/Platform/page.mdx) / ReloadApp

```ts
type ReloadApp = (url?: string) => void;
```

Defined in: [packages/common/src/Platform.ts:55](https://github.com/SQLoot/EvoLoot/blob/d86280a21ced8e9f86b70217b1c345b37bea4ce2/packages/common/src/Platform.ts#L55)

Reload the app in a platform-specific way.

Use this after purging persistent storage to clear in-memory state and ensure
the app starts fresh. It does not purge storage itself.

- **Web**: Redirects to the specified URL (defaults to `/`)
- **React Native**: Restarts the app (URL ignored)

## Parameters

| Parameter | Type |
| ------ | ------ |
| `url?` | `string` |

## Returns

`void`
