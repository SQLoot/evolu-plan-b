[API Reference](/docs/api-reference/page.mdx) / [@evolu/nodejs](/docs/api-reference/nodejs/page.mdx) / createRunner

```ts
const createRunner: CreateRunner<RunnerDeps & ShutdownDep>;
```

Defined in: [nodejs/src/Task.ts:60](https://github.com/SQLoot/EvoLoot/blob/d86280a21ced8e9f86b70217b1c345b37bea4ce2/packages/nodejs/src/Task.ts#L60)

Creates [Runner](/docs/api-reference/common/Task/interfaces/Runner.mdx) for Node.js with global error handling and graceful
shutdown.

Registers `uncaughtException` and `unhandledRejection` handlers that log
errors and initiate graceful shutdown. Adds a `shutdown` promise to deps that
resolves on termination signals (`SIGINT`, `SIGTERM`, `SIGHUP`). Handlers are
removed when the Runner is disposed.

### Example

```ts
const console = createConsole({
  formatter: createConsoleFormatter()({
    timestampFormat: "relative",
  }),
});

const deps = { ...createRelayDeps(), console };

await using run = createRunner(deps);
await using stack = run.stack();

await stack.use(startRelay({ port: 4000 }));

await run.deps.shutdown;
```
